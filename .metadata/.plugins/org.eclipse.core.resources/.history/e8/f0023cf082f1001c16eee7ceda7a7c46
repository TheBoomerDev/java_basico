package test.baraja;

public class Juego {

	public static void main(String[] args) {
		
		Baraja baraja = new Baraja();
		
		// While
		int numeroCartas = 0;
		while (numeroCartas < 5) {
			Carta carta = new Carta(Carta.PALO.CORAZON, 1+numeroCartas); 
			baraja.addCarta(carta);
			numeroCartas++; // numeroCartas = numeroCartas + 1
		}
		
		// DO-While
		numeroCartas = 0;
		do {
			Carta carta = new Carta(Carta.PALO.DIAMANTE, 1+numeroCartas); 
			baraja.addCarta(carta);
			numeroCartas++; // numeroCartas = numeroCartas + 1
		}while(numeroCartas < 5);
		
		// FOR
		numeroCartas = 5;
		for (int i = 0; i < numeroCartas; i++) {
			Carta carta = new Carta(Carta.PALO.ROMBO, 1+i); 
			baraja.addCarta(carta);
		}
		
	}
}
